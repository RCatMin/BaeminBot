# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# 단, 값이 안 들어 있는 예시 파일은 올려야 다른 사람이 뭘 채워야 할지 압니다.
!.env.local.example

# 로컬 SQLite DB — 참여자 계좌번호가 들어 있으므로 절대 커밋 금지
/data

# 사내 공유용으로 쓰고 버리는 소개 문구 초안. 저장소에 남길 내용이 아닙니다.
/audience-tailored-intros.md

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
